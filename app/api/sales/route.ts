// Phase 5 — Sales API routes
// POST: confirm sale (delegates to confirm_sale RPC)
export async function GET() {
  return Response.json({ message: 'Sales API — Phase 5' })
}

export async function POST() {
  return Response.json({ message: 'Sales API — Phase 5' })
}
